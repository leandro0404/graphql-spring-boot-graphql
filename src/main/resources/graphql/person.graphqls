type Person {
    id : ID,
    name : String,
    age : Int,
    gender :Gender,
    documents : [Document]
}

enum Gender{
    MALE , FEMALE
}

input CreatePersonInput
{
    name: String,
    age: Int,
    gender: Gender
}